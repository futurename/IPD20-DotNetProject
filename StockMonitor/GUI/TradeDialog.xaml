<Window x:Class="GUI.TradeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:GUI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
        mc:Ignorable="d"
        xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=PresentationFramework"
        Title="TradeDialog" Height="353.2" Width="672.87">
    <Window.Resources>
        <Style TargetType="{x:Type UserControl}">
            <Style.Triggers>
                <Trigger Property="componentModel:DesignerProperties.IsInDesignMode" Value="True">
                    <Setter Property="Background" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="10" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.8*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <materialDesign:Card
                    Grid.Row="0"
                    Grid.ColumnSpan="3"
                    Background="{DynamicResource PrimaryHueLightBrush}"
                    Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                    Padding="8"
                    UniformCornerRadius="6">

                <StackPanel Orientation="Horizontal" Margin="0,0,0,-5.8">
                    <Label Content="Symbol:" HorizontalAlignment="Left" Margin="0,0,0,0.2"/>
                    <TextBlock Name="txtSymbol" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Symbol" VerticalAlignment="Top" Margin="0,4,0,0"/>
                    <TextBlock Name="txtPrice" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Price" VerticalAlignment="Top" Margin="20,4,0,0"/>
                    <TextBlock Name="txtPriceChange" HorizontalAlignment="Left" TextWrapping="Wrap" Text="PriceChange" VerticalAlignment="Top" Margin="10,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal">
                <RadioButton Style="{StaticResource MaterialDesignTabRadioButton}"
                            Margin="4"
                            IsChecked="True"
                            Content="BUY" />
                <RadioButton Style="{StaticResource MaterialDesignTabRadioButton}"
                            Margin="4"
                            IsChecked="False"
                            Content="SELL" />
            </StackPanel>
            <ComboBox Grid.Row="2" Grid.Column="0" materialDesign:HintAssist.Hint="Order Type">
                <ComboBoxItem>
                    Limit Order
                </ComboBoxItem>
                <ComboBoxItem>
                    Stop Order
                </ComboBoxItem>
            </ComboBox>
            <TextBox Grid.Row="2" Grid.Column="2"   
                     materialDesign:HintAssist.Hint="Quantity"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     VerticalAlignment="Center" Height="44" Margin="0,3.6,0,18.8" />
            
            <TextBlock Text="Target Price" Grid.Row="3" Grid.ColumnSpan="3"
                       Style="{StaticResource MaterialDesignTextBlock}"/>
            <TextBox Grid.Row="3" Grid.Column="0"   
                     materialDesign:HintAssist.Hint="Limit Price"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     VerticalAlignment="Center" />
            <TextBox Grid.Row="3" Grid.Column="2" 
                     materialDesign:HintAssist.Hint="Stop Price"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     VerticalAlignment="Center" />
            
            <TextBlock Text="Time in force (Until)" Grid.Row="4" Grid.ColumnSpan="3"
                       Style="{StaticResource MaterialDesignTextBlock}"/>
            <DatePicker Grid.Row="4" Grid.Column="0"
                     materialDesign:HintAssist.Hint="Pick Date"
                     DisplayDateStart="{x:Static sys:DateTime.Today}"
                     Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            <materialDesign:TimePicker Grid.Row="4" Grid.Column="2"
                      Is24Hours="True"
                      x:Name="PresetTimePicker"
                      Margin="0,18.4,0,6.2" />
            <Button Grid.Row="5" Grid.Column="0"
                      Style="{StaticResource MaterialDesignRaisedButton}"
                      Width="100"
                      ToolTip="Resource name: MaterialDesignRaisedButton"
                      Content="Trade" />
            <Button Grid.Row="5" Grid.Column="2"
                      Style="{StaticResource MaterialDesignRaisedLightButton}"
                      Width="100"
                      ToolTip="Resource name: MaterialDesignRaisedLightButton"
                      Content="Cancel" />
        </Grid>

        <Grid Grid.Column="1" Margin="10">
            <ListView>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Symbol"  />
                        <GridViewColumn Header="Trade"  />
                        <GridViewColumn Header="Limit"  />
                        <GridViewColumn Header="Stop"  />
                        <GridViewColumn Header="Time"  />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
