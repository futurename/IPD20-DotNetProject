<UserControl x:Class="GUI.CandleChartUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI"
             mc:Ignorable="d" 
             xmlns:Wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             d:DesignHeight="663.442" d:DesignWidth="671.319">
    <Grid>
        <TextBlock>
            <TextBlock >Stock Price: $</TextBlock>
            <TextBlock Name="Y"></TextBlock>
        </TextBlock>
        <Wpf:CartesianChart Name="chartStockPrice" Series="{Binding SeriesCollection}" 
                            Zoom="X"
                            Hoverable="False" 
                            DataTooltip="{x:Null}"
                            MouseMove="ChartMouseMove"
                            DataClick="ChartOnDataClick"
                            DataHover="Chart_OnDataHover" ZoomingSpeed="0" Cursor="Cross" MouseEnter="chartStockPrice_MouseEnter">
            <Wpf:CartesianChart.AxisX>
                <Wpf:Axis Labels="{Binding Labels}" 
                          MinValue="{Binding Min, Mode=TwoWay}" 
                          MaxValue="{Binding Max, Mode=TwoWay}"
                          RangeChanged="Ax_RangeChanged"
                          PreviewRangeChanged="Axis_OnPreviewRangeChanged"/>
            </Wpf:CartesianChart.AxisX>
        </Wpf:CartesianChart>
        <Label Name="lbl_Y_Axis" HorizontalAlignment="Left" Margin="26,220,0,0" VerticalAlignment="Top" Foreground="Gray" Background="Gray" BorderBrush="Gray" Height="1" Panel.ZIndex="-1"/>
        <Label Name="lbl_X_Axis" HorizontalAlignment="Left" Margin="200,10,0,0" VerticalAlignment="Top" Width="1" Foreground="Gray" Background="Gray" BorderBrush="Gray"  Panel.ZIndex="-1"/>
        <TextBlock Name="txt_X_Axis" HorizontalAlignment="Left" Height="23" Margin="144,631,0,0" TextWrapping="Wrap" Text="X-axis" VerticalAlignment="Top" Width="84" />
        <Button Name="btReload" Content="Reload" HorizontalAlignment="Left" Margin="500,10,0,0" VerticalAlignment="Top" Width="75" Click="btReload_Click" RenderTransformOrigin="-0.308,0.902" />
    </Grid>
</UserControl>
